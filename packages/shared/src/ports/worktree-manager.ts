/**
 * IWorktreeManager - Port interface for Git worktree operations
 *
 * Re-exports from types/worktree.ts which already defines IWorktreeManager.
 * This port file exists for consistency and to establish the pattern.
 */

export type { IWorktreeManager, Worktree } from "../types/worktree";
