/**
 * IStore - Port interface for persistent storage
 *
 * Re-exports from types/store.ts which already defines IStore.
 * This port file exists for consistency and to establish the pattern.
 */

export type { IStore } from "../types/store";
